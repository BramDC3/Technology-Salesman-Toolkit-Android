package com.bramdeconinck.technologysalesmantoolkit.models

/**
 * Enum with all categories a Service object could possibly have.
 */
enum class Category {
    Windows,
    Android,
    Apple,
    Andere
}